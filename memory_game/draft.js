function gettheid(x) {
    if (compir.length < 2) {
        n++
        compir[n] = $($(x)).attr("id");
        if (compir[0] == compir[1]) {
            alert('good memory')
        } else {


        }
    }

    console.log(compir);
} 